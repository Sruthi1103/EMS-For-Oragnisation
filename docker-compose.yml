version: "3"
services:
  backend:
    image: "sruthidoc/backend"
    mem_limit: 128m
    hostname: backend
  frontend:
    image: "sruthidoc/frontend"
    mem_limit: 128m
    hostname: frontend
  nginx:
    restart: always
    mem_limit: 128m
    hostname: nginx
    ports:
      - "80:80"
